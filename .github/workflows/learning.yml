name: learning actions
on: push
jobs:
  build:
    name: hello world action
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: ./action-a
      with:
        MY_NAME: "Mona"
